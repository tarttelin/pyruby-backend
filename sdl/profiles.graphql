extend type Query {
    myProfile: Person!
}

type Person {
    id: ID!
    name: String!
    users: [UserConnection!]
    contactInfo: ContactInfo!
    photoUrl: String!
}

type ContactInfo {
    email: String!
}